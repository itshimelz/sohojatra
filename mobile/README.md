# Sohojatra Mobile (React Native Scaffold)

This scaffold enables offline-first mobile development for Sohojatra.

## Stack
- Expo + React Native
- AsyncStorage for offline queue
- API sync to `/api/concerns/sync`

## Commands
- `npm install`
- `npm run start`
- `npm run android` or `npm run ios`
